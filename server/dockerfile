# Use an official Python runtime as a parent image
FROM python:3.9-slim

# Set the working directory in the container
WORKDIR /app

# Copy the requirements file into the container
COPY requirements.txt .

# Install any needed packages specified in requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of the application code into the container
COPY . .

# Create the initial 'uploads' and data persistence directories
RUN mkdir -p /app/uploads

# Expose the port the app runs on (Gunicorn will run on port 5000)
EXPOSE 5000

# Use Gunicorn to run the app. This is a production-ready server.
# It will run the 'app' object from the 'app.py' file.
CMD ["gunicorn", "--workers", "3", "--bind", "0.0.0.0:5000", "app:app"]